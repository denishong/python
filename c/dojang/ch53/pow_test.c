#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

struct Point2D {
    int x;
    int y;
};

int main()
{
	
	double a = sqrt(pow(10.0,2.0) + pow(10.0,2.0));
	printf("a : %f \n",a*3);


    return 0;
}
